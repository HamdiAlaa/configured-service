import * as pulumi from "@pulumi/pulumi";

const config = new pulumi.Config();
// export const repo = config.require('repo');
// export const chart = config.require('chart');
// export const version = config.require('version');
// export const release_name = config.require('release_name');